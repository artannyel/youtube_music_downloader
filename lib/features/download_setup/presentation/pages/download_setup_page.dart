import 'package:flutter/material.dart';

class DownloadSetupPage extends StatelessWidget {
  const DownloadSetupPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Download Setup Page'),
      ),
    );
  }
}
